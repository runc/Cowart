// Empty stub for Node-only modules (diagnostics_channel, async_hooks) that the
// AI SDK references behind an isNodeRuntime() guard. They never execute in the
// browser; this stub only exists to keep the production bundle free of Node
// externalization warnings.
export default undefined
